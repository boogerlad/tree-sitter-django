==================
for empty verbatim
==================

{% for x in items %}
  {{ x }}
{% empty %}
  {% verbatim %}{{ x }}{% endverbatim %}
{% endfor %}

---

(template
  (content)
  (block
    (for_block
      (loop_targets
        (loop_target))
      (filter_expression
        (primary_expression
          (lookup
            (identifier))))
      (content)
      (interpolation
        (filter_expression
          (primary_expression
            (lookup
              (identifier)))))
      (content)
      (content)
      (block
        (verbatim_block))
      (content)))
  (content))
